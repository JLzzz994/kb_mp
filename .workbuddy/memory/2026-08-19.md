# 2026-08-19

- 基于 docs/ 下 PRD V1.0、概要设计总纲 V1.0、数据对象文档、接口约定文档，用 diagram-builder skill 绘制 4A 架构总览图（业务/应用/数据/技术四层 SVG），落盘至 `docs/4A架构图.svg`。
- 图内容要点：三角色 + 五大业务能力；前端 6 模块 + 后端 6 服务（含分层依赖 strip：api→services→workflows(LangGraph)→domain→repositories→infrastructure）；数据四域 11 表 + MySQL/Milvus/Redis；技术栈以 chip 形式呈现（前端/后端/中间件与工程三组）。
- 依据 docs/audit/风险报告.md §10（20 条 4A 图审计项）+ ADR-0002（LangGraph 8 节点）+ ADR-0007（/api/v1 路径冻结）+ 需求 §2.9.6（后端 8 服务）+ 原型设计说明（深海权限台视觉规范），重绘 4A 架构图 V2 并覆写 `docs/4A架构图.svg`（viewBox 680×1490，五层视图）。
- V2 关键修复：后端拆为 8 个独立服务盒（A1）；新增第 ⑤ 层质量属性（性能/可用性/可观测性/可维护性，A2）；业务层补 4 类关键场景带阈值（A3）；技术栈补 aiomysql/langchain-milvus/BackgroundTasks/Vue Router 4/Attu/pytest 版本号（A4）；数据域显式标注 chat_sessions（A5）；应用层补 check-permissions 调用链 + 43 REST 接口映射 strip（A6）；节点数统一为 LangGraph 8（A7）；④⑤ 层间加双向虚线追溯箭头。
- 注意：docs/adr/ 下无 ADR-0001 文件（仅 0002/0007）；其"MySQL+Milvus+Redis 单机部署"决策按概要设计 §12 索引体现。
