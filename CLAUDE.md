## Agent skills

### Issue tracker

GitHub issues via `gh` CLI. See `docs/agents/问题追踪.md`.

### Triage labels

Default five-role vocabulary (needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix). See `docs/agents/分流标签.md`.

### Domain docs

Single-context layout — root `CONTEXT.md` + `docs/adr/`. See `docs/agents/领域文档.md`.

### 规范
项目用uv管理
git提交时中文描述,提交远程仓库由我进行

### 后端要求
能用数据模型就少用字典
single-agent-blueprint(电商小二代码沉淀的skill)
multi-agent-blueprint(舆情分析沉淀的skill)
ponytail(github上高星减少冗余代码的skill)